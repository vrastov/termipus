{
  "name": "termipus-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:chrome": "TARGET=chrome node build.js",
    "build:firefox": "TARGET=firefox node build.js",
    "watch": "node build.js --watch",
    "test": "jest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/firefox-webext-browser": "^120.0.0",
    "@types/jest": "^29.5.0",
    "esbuild": "^0.20.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@xterm/xterm": "^5.3.0",
    "@xterm/addon-fit": "^0.10.0"
  }
}
